#pragma once

#include <stdint.h>
#include <string>


namespace tge {
  /*
  const uint32_t LINEAR = 1UL << 0;

  struct Tween {
    int function_ref = -1;
    string variable;
    float start = 0;
    float end = 1;
    uint32_t duration = 0;
    uint32_t elapsed = 0;
  };

  int lua_tween(lua_State* L);
  void update_tweens(lua_State* L, uint32_t time);*/
}