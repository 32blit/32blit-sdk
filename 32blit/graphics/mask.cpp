
namespace blit {
  /*
  void mask_combine(uint8_t *s, uint8_t *d, uint8_t mode) {
    // TODO: implement other modes

    uint8_t *e = d + 160 * 120;
    while (d < e) {
      *d++ = *d > *s ? *d : *s;
      s++;
    }
  }*/

}  