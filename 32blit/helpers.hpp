#pragma once
/*
#undef min
#undef max

template<typename T>
const T& clamp(const T &value, const T &min, const T &max) {
  return value < min ? min : (value > max ? max : value);
}

template<typename T>
const T& min(const T &v1, const T &v2) {
  return v1 < v2 ? v1 : v2;
}

template<typename T>
const T& max(const T &v1, const T &v2) {
  return v1 > v2 ? v1 : v2;
}

template<typename T>
const T& min(const T &v1, const T &v2, const T &v3) {
  return min(v1, min(v2, v3));
}

template<typename T>
const T& max(const T &v1, const T &v2, const T &v3) {
  return max(v1, max(v2, v3));
}*/